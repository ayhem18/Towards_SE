# include <iostream>

// int getInteger();

// void bool_data_type();

// double getNumber();
// char getOperation();
// double calculate(double op1, double op2, char op);

int mod(int a, int b);

int main() {    
    // double x1 = getNumber();
    // double x2 = getNumber();
    // char op = getOperation();
    // if (op == '*' | op == '/' | op == '-' | op == '+') {
    //     std:: cout << x1 << ' ' << op << ' ' << x2 << " = " << calculate(x1, x2, op); 
    // }
    int x {0};
    int y {0};
    // std:: cout << y << x << '\n';

    std:: cout << mod(10, 2) << '\n';

    // while (1){ //while True
    //     std:: cout << "please enter the first number (the one to be divided)\n"; 
    //     if (std::cin >> x) {
    //         std::cout << "please enter the second number (the one to divide on)\n";
    //         if (std:: cin >> y) {
    //             std::cout << "The modulo of " << x << " with respect to " << y << " "  << (mod(x, y)) << "\n";
    //         }
    //         else {
    //         std::cout << "Invalid Input, aborting !!\n"; 
    //         break;
    //         }
    //     }

    //     else {
    //         std::cout << "Invalid Input, aborting !!\n"; 
    //         break;
    //     }     
    // }
}
